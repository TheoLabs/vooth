/**
 * 정규화 크롭 영역(0~1, 원본 이미지 대비 비율).
 * 썸네일·컷 등 "원본의 어느 영역을 표시할지"를 지정할 때 쓴다(잘라낸 사본 미저장, 표시 시 유도).
 * - `x`, `y`: 좌상단 좌표(0~1)
 * - `w`, `h`: 폭/높이(0~1)
 * 제약: 각 값 0~1, `x + w ≤ 1`, `y + h ≤ 1`, `w > 0`, `h > 0`.
 */
export interface CropBox {
  x: number;
  y: number;
  w: number;
  h: number;
}
